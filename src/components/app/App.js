import Router from "../../router/Router";
import MainContainer from "../mainContainer/MainContainer";

function App() {
  return (
    <MainContainer>
      <Router/>
    </MainContainer>
  );
}

export default App;